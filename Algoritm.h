#pragma once
#include <vector>//Библиотека подключения вектора
#include <map>//Библиотека подключения словаря
#include <string>//Библиотека подключения строк

void CharCountAlghoritm(std::map<std::string, unsigned> *chars,std::vector<std::string> text);//Функция счёта символов текста
